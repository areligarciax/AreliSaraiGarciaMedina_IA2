# Areli Sarai García Medina | 20310380

tupla = (3, 1, 4, 1, 5, 9, 2, 6, 5, 3, 5)

# Convertir la tupla en una lista, ordenarla y convertirla de nuevo en una tupla
nueva_tupla = tuple(sorted(list(tupla)))
print(nueva_tupla)